# Declarative Table

The Data section controls generated rows and scale.
The Columns section decides whether the squared column is included.
Status cards mirror the current parameter values without executing plugin UI code.
