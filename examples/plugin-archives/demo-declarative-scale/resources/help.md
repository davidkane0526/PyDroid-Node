# Declarative Scale

This help text is packaged as a plugin resource and rendered by the host.
The plugin declares groups, status items and help metadata only; it does not inject DOM or React components.
